[project]
name = "zep"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "numpy>=1.26.0",
    "pyyaml>=6.0",
    "sentence-transformers>=3.0.0",
]

[dependency-groups]
dev = ["pytest>=8.0.0"]
